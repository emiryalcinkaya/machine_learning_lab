"""
Create and access NumPy arrays.
"""

import numpy as np

numbers = np.array([10, 20, 30, 40, 50])

print("Array:", numbers)
print("First Element:", numbers[0])
print("Last Element:", numbers[-1])
print("Length:", len(numbers))